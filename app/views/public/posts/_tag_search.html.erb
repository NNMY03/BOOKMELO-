<div class="card accordion-container">
  <div class="card-body accordion-title js-accordion-title">
    感情タグから探す
  </div>
  <div class="card-body accordion-content">
    <% tags.each do |tag| %>
      <%= link_to tag.name, books_path(tag_id: tag.id) %>
      <% end %>
      <%= link_to "＃ALL", books_path %>
  </div>
</div>